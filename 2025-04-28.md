## try-catch-finallyとは

### 1.try
エラーが発生するかもしれないコードを書く場所
通常、非同期処理(API呼び出し)やリスクのある処理

### 2.catch
エラーが起こった時の処理を書く場所

### 3.finally
成功しても失敗しても必ず実行される処理
例えば
・ローディング状態を解除
・接続を閉じる
・後処理をする

エラーがあっても必ず走るのがポイント